---
import { ExclamationCircleIcon } from "@heroicons/react/24/outline";

interface Props {
  errorId: string;
}
const { errorId } = Astro.props;
---

<span
  class="flex gap-1 items-center text-maroon opacity-0 transition-opacity duration-300"
>
  <ExclamationCircleIcon className="hidden invisible size-6" />
  <span id={errorId} class="transition-all"></span>
</span>
