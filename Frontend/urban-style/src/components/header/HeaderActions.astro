---
import CartAnchor from './CartAnchor.astro'
import LoginModal from './LoginModal.astro'
import UserMenu from './UserMenu.astro'
import Button from '../Button.astro'

const { user } = Astro.locals
---

<ul
	class="flex h-full flex-col-reverse items-center gap-x-4 gap-y-3 px-2 max-md:w-full md:flex-row"
>
	<li class="w-full md:w-fit">
		<CartAnchor />
	</li>
	<li class="group w-full md:w-fit">
		{
			!user && (
				<>
					<Button id="login_button" size="sm">
						<span class=""> Iniciar Sesión </span>
					</Button>
					<LoginModal />
				</>
			)
		}
		{user && <UserMenu user={user} />}
	</li>
</ul>
