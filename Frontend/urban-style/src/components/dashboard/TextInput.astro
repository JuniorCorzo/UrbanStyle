---
import type { FieldProperties } from "@/interface/form-mediator.interface";
import LabelInput from "./LabelInput.astro";

interface Props extends Omit<FieldProperties, "options" | "isMultiple"> {}

const { label, name, placeholder, value, required } = Astro.props;
---

<LabelInput label={label}>
  <input
    class="border border-border rounded px-2 py-1 focus:outline-none focus:ring-2 focus:ring-accent text-text"
    type="text"
    id={name}
    name={name}
    placeholder={placeholder}
    value={value}
    required={required}
  />
</LabelInput>
